micronaut:
  application:
    name: gfo-processing
  config-client:
    enabled: true

kubernetes:
  client:
    secure: true
    #namespace: gegd-camel
    secrets:
      enabled: false
    discovery:
      enabled: true
    config-maps:
      enabled: true
      watch: true
      labels:
        - app: gfo-processing